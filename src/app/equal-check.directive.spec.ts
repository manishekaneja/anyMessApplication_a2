import { EqualCheckDirective } from './equal-check.directive';

describe('EqualCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualCheckDirective();
    expect(directive).toBeTruthy();
  });
});
